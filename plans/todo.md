# TODO

These items potentiall can be addressed at a later date:

- Remove `config.py` as it only stores Jira ticket information and we can pass that it manually
- Remove old scripts and migration guide entirely
